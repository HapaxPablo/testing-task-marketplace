{
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "server": "json-server --watch db.json --port 8000",
    "dev": "yarn --cwd ./testing-task-marketplace dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
